---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={["container mx-auto px-3 md:px-4 lg:px-5 py-3 flex flex-col gap-4", className]}>
  <slot />
</div>
